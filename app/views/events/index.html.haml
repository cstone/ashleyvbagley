%ol.breadcrumb
  %li= link_to 'Home', root_path
  %li.active Upcoming Events
%h3 Upcoming Events
%br/
- @events.each do |event|
  %h4= link_to event.title, event
  %p
    %strong= event.time.to_formatted_s(:my_format)
  %p= event.venue
  %p= event.description.truncate(250).html_safe
  %p= link_to 'More Info', event, class: 'btn btn-sm btn-primary pull-right'
  %br/
  %hr/
= paginate @events