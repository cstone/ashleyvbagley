- if !current_page?(root_path)
  %ol.breadcrumb
    %li= link_to 'Home', root_path
    %li.active= @page.title
- if !current_page?(root_path)
  %h4= @page.title
- if @page.page_image.url
  = image_tag @page.page_image.url, class: 'img-responsive img-float-right'
= @page.content.html_safe